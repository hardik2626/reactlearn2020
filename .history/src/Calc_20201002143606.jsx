import React from 'react';

function add(a, b) {
    let sum = a + b;
    return sum;
}
export default add;

function sub(a, b) {
    let sub = a - b;
    return sub;
}
export default sub;

function mult(a, b) {
    let mult = a * b;
    return mult;
}
export default mult;

function div(a, b) {
    let div = a / b;
    return div;
}
export default div;